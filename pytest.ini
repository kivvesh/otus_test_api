[pytest]

; log_cli_level = INFO
; log_file = logs/test.log
; log_format = %(asctime)s %(levelname)s %(message)s
; log_date_format = %Y-%m-%d %H:%M:%S

addopts =
    -v
    -s

markers =
    smoke

    dog_ceo
    openbrewerydb
    jsonplaceholder

    test_module

pythonpath = /home/kivvesh/otus/otus_test_api
